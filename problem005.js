// Karol Cieślik PGK2

function strangeSum(a, b) {
  if( (Number.isInteger(b) === false) || (Number.isInteger(b) === false))
    return null;
  
  if (a == b)
    return (a+b)*3;
  else
    return a+b;
};